services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: rivvy_portal
      POSTGRES_USER: rivvy
      POSTGRES_PASSWORD: rivvy
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
