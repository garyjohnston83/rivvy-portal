server:
  port: 9090
  servlet:
    session:
      timeout: 30m

management:
  endpoints:
    web:
      exposure:
        include: health

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rivvy_portal
    username: rivvy
    password: rivvy
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml
  session:
    store-type: jdbc
    jdbc:
      initialize-schema: always

auth:
  lockout:
    threshold: ${AUTH_LOCKOUT_THRESHOLD:5}
    window-minutes: ${AUTH_LOCKOUT_WINDOW_MINUTES:15}
    duration-minutes: ${AUTH_LOCKOUT_DURATION_MINUTES:30}
